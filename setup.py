# setup.py for the logic puzzles project

from setuptools import setup, find_packages

setup(
    name='logicpuzzles',
    version='0.1',
    packages=['logicpuzzles'],
    install_requires=['hwtypes'],
)
